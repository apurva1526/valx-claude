# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Pinned to SDK 54 (not the latest) to match the installed Expo Go client version on the dev device — don't bump without checking Expo Go compatibility first.
